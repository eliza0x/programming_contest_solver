#include <vector>
#include <stdio.h>

int n;
vector<int> imos;

int main() {
    while (cin >> n, n) {
        vector<int> ride(3);
        vector<int> left(3);
        for (int i=0; i<n; i++) {
            scanf("%d:%d:%d", ride[0], ride[1], ride[2]);
            scanf("%d:%d:%d", left[0], left[1], left
                    :w2]);
        }
    }
}
