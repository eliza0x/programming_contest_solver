#include <iostream>
#include <vector>
using namespace std;

int main() {
    int n, a;
    vector<int> xs(n);
    for (auto &x: xs) cin >> x;

    vector<vector<bool>> memo(n+1, vector<bool>(a+1, false));
    for (int j=1; j<n+1; n++) {
    }
}
